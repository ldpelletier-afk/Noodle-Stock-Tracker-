# Full dependency set for the combined app (app.py).
# Installing only the Tracker half (portfolio_app.py)? Use requirements-core.txt.
# Installing only the Analytics half (analytics_app.py)? Use requirements-analytics.txt.
-r requirements-analytics.txt
